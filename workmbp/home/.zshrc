# Add .local/bin to PATH (for dbt and dbtf)
export PATH="$PATH:$HOME/.local/bin"

# Add libpq (PostgreSQL client) to PATH
export PATH="/opt/homebrew/opt/libpq/bin:$PATH"

source /Users/rash/.config/broot/launcher/bash/br
