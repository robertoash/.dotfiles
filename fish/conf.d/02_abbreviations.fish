# Abbreviations

# Apps
abbr nv nvim
abbr yz yazi
abbr lg lazygit
abbr lzd lazydocker

## Buku Bookmarks
abbr bka "buku --suggest -w" # Buku add (interactive)
abbr bkd "buku --suggest -d" # Buku delete
abbr bke "buku --suggest -w"
abbr bkl "buku --suggest -p" # Buku list
abbr bkll "buku --suggest -l" # Buku lock
abbr bklu "buku --suggest -k" # Buku unlock
abbr bks "buku --suggest --np -s" # Buku search (no prompt)
abbr bksp "buku --suggest -s" # Buku search (prompt)
abbr bkw "buku --suggest -w --tag ''" # Buku write

### Buku Database Functions
abbr bk_c "switch_buku_db current"
abbr bk_p "switch_buku_db rashp"
abbr bk_r "switch_buku_db rash"
abbr bk_s "switch_buku_db"

# NZBGet
abbr nzbget "nzbget -c ~/.config/nzbget/nzbget.conf"

# Shell
abbr fish_reload "source ~/.config/fish/config.fish"
abbr snk_here "snake_case_all ."

## Package Management
abbr yay_total "yay -Syu --devel"
abbr Syu "yay_update"

# System
abbr free_space "lsblk -o NAME,SIZE,FSAVAIL,MOUNTPOINT"
abbr mem "smem -t -k -c 'name rss' | tail -25"

### SGPT
abbr gs "sgpt -s --no-interaction"
abbr gsi "sgpt -s --interaction"
abbr gd "sgpt -d"
abbr gtc "sgpt -c"

# Fd
abbr fd "fd -H"
abbr fdf "fd -H -t f"
abbr fdd "fd -H -t d"

# Jc
abbr jc "jc -p"

# Qalc
abbr calc "qalc"

## Dust
abbr dust "dust -r"

## Rsync
abbr rs "rsync -ah --itemize-changes --partial --progress --stats"
abbr rsdry "rsync -ah --itemize-changes --progress --stats --dry-run"
abbr rsmv "rsync -ah --progress --stats --remove-source-files"

## Atuin
abbr atuin_clean "atuin history prune"

## Frecent
abbr frecent_cleanup "~/.config/scripts/shell/frecent_cleanup.py"
## SVT Text
abbr svtti "svttext -colors -interactive"
abbr svttc "svttext -colors"
abbr svttci "svttext -colors -interactive"
abbr svttic "svttext -colors -interactive"
# Gcalcli
abbr gcal "gcalcli"
abbr gcala "gcalcli agenda"
## Spotify_player
abbr spt "spotify_player"
abbr xeyes "xprop"

