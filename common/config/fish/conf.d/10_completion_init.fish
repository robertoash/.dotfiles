# Initialize smart completion system
# Must run before keybindings that use __completion_get_type

# Source completion config (contains global vars + __completion_get_type function)
source ~/.config/fish/functions/__completion_config.fish
