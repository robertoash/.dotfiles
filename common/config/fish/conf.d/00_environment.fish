# ~/.config/fish/conf.d/00_environment.fish
# Environment Variables Configuration
#
# NOTE: Most environment variables are now managed by home-manager sessionVariables
# See: ~/.dotfiles-nix/home-manager/deploy-tools/session-variables.nix
#
# This file only contains Fish-specific runtime configuration

# Secrets are loaded via sops-nix (see 01_load_sops_scrts.fish)
