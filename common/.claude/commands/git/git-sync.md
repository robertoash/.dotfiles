Commit and push all changes in all project repos (working directory + additional working directories) in logical commit groups until all repos are clean.

- Group changes by feature/fix/purpose
- Run `bd sync` before and after commits if beads exists
- Push all changes to remote
- Verify all repos show clean status
