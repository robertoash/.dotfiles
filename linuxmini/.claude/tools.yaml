hass-mcp:
  type: stdio
  command: docker
  args: ["run", "-i", "--rm", "-e", "HA_URL", "-e", "HA_TOKEN", "voska/hass-mcp"]
  env:
    HA_URL: "http://10.20.10.100:8123"
    HA_TOKEN: "{{ha-token-global}}"
  permissions:
    allow:
      - "mcp__hass-mcp__*"
